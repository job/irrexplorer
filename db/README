How to setup to postgres for irrexplorer and load data into it.

First create a database for IRRExplorer:

$ sudo su - postgres
$ createdb irrexplorer

Then load the schema

psql irrexplorer -f ../data/schema.sql

Run bootstrap to download IRR databases and load up database.

Read bootstrap if you are interested in the details :-)

